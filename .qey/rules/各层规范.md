# 各层规范(Layer Conventions)

> 本项目各层(入口 → 业务 → 数据)的写法约定。qey-init 扫描后填充;后续 /recap 增量更新。
> 配套:`rules/git提交规范.md`(提交)、`rules/历史遗留陷阱.md`(改X 碰哪些文件)。

## 通用

- [qey-init 填充:命名约定(驼峰/下划线/中英)]
- [qey-init 填充:错误/异常处理归属哪层(谁抛、谁转、谁兜底)]
- [qey-init 填充:日志规范(关键节点打什么)]
- 注释规范:见 `代码注释规范.md`(克制、无 AI 味,只注 why)
- 安全/错误/日志/外部调用:见 `安全规范.md` / `错误处理规范.md` / `日志规范.md` / `外部调用规范.md`(通用防御底线)

## 入口层(Controller / Route / Handler)

- [qey-init 填充:只做参数校验 + 转发,不写业务]
- [qey-init 填充:统一响应格式(成功/错误 JSON 结构)]

## 业务层(Service / Logic / UseCase)

- [qey-init 填充:业务规则在这层;不返回 HTTP、不直接 echo/exit]
- [qey-init 填充:事务边界在哪(方法级/手动)]
- [qey-init 填充:外部调用(队列/RPC)放这层]

## 数据层(Repository / Model / DAO)

- [qey-init 填充:只管持久化;不含业务判断]
- [qey-init 填充:查询规范(防 N+1 / 必走索引 / 软删除)]

## 视图/前端层(如有)

- [qey-init 填充:组件/样式约定]
