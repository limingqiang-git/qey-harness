---
description: 会话日志(机械) — bash log.sh 抓 git 信息 append 到 journal;30 秒,不提炼不判断不删旧
allowed-tools: Bash, Read
---
# qey-log(Claude adapter → canonical)

> Claude 专属薄包装。canonical 命令逻辑在 `.qey/commands/qey:log.md`(平台无关)。
> 读它的 body 并执行(忽略本文件 body,以 canonical 为准)。本文件只放 Claude 专属 frontmatter(`allowed-tools`)。

$ARGUMENTS
