---
description: 知识提炼(刻意) — 7类触发判定 → staging → 落点候选(带范围检查)→ Ask 确认 → 落盘;默认无可提炼
argument-hint: "[deep]"
allowed-tools: Bash, Read, Write, Edit, Grep, AskUserQuestion
---
# qey-distill(Claude adapter → canonical)

> Claude 专属薄包装。canonical 命令逻辑在 `.qey/commands/qey:distill.md`(平台无关)。
> 读它的 body 并执行(忽略本文件 body,以 canonical 为准)。本文件只放 Claude 专属 frontmatter(`allowed-tools`)。

$ARGUMENTS
