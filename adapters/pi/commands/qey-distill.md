---
description: 知识提炼(刻意) — 7类触发判定 → staging → 落点候选(带范围检查)→ Ask 确认 → 落盘;默认无可提炼
argument-hint: "[deep]"
---
# qey-distill(omp/pi adapter → canonical)

> omp(oh-my-pi)专属薄包装,native provider 发现(`.omp/commands/qey:distill.md`)。
> canonical 命令逻辑在 `.qey/commands/qey:distill.md`(平台无关)。
> 读它的 body 并执行(忽略本文件 body,以 canonical 为准)。
> omp 无 `allowed-tools` 概念,工具策略由 `.omp/config.yml` 的 `tools.approval` + `bash.patterns` 控制。

$ARGUMENTS
