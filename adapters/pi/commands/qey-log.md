---
description: 会话日志(机械) — bash log.sh 抓 git 信息 append 到 journal;30 秒,不提炼不判断不删旧
---
# qey-log(omp/pi adapter → canonical)

> omp(oh-my-pi)专属薄包装,native provider 发现(`.omp/commands/qey:log.md`)。
> canonical 命令逻辑在 `.qey/commands/qey:log.md`(平台无关)。
> 读它的 body 并执行(忽略本文件 body,以 canonical 为准)。
> omp 无 `allowed-tools` 概念,工具策略由 `.omp/config.yml` 的 `tools.approval` + `bash.patterns` 控制。

$ARGUMENTS
